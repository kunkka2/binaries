name: Simple Hello World
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello, World!"
      - name: Trace ip
        run: apt install nexttrace -y && nexttrace -4 104.160.37.173
